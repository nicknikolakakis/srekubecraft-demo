apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: pod-watcher-trigger
  namespace: default
spec:
  broker: default
  filter:
    attributes:
      type: dev.knative.apiserver.resource.update
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: event-display
