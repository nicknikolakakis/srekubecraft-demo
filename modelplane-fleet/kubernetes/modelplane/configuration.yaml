# Pinned here, not pulled from upstream docs: that manifest lags a release at every tag (v0.3.1's copy still points at v0.3.0, which ships a CEL rule the API server rejects).
apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: modelplane
spec:
  package: xpkg.upbound.io/modelplane/modelplane:v0.3.1
